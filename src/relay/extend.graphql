# Add your schema type extensions below
# For example:
# extend type RootQueryType {
#   listOfPeople: [People!]! @listLength(min: 1, max: 5)
# }
extend type Student {
  fullName: String
}
# type People {
#   name: String! @fake(type: name)
#   organization: String! @examples(values: ["gitstart"])
# }

# Use the following directives for overriding the fake data generated by the mock server.
# @fake: overrides the type (faker.js type) of fake data generated for a particular field.
# @listLength: overrides the number of items in a list.
# @examples: overrides the default mock data with your own examples of values.
