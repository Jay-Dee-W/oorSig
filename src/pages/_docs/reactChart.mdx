import { Chart } from './chartConfig.js';
import Head from 'next/head';
import { Line } from 'react-chartjs-2';
import { MDXWrapper } from '@elements/MDXWrapper';

<MDXWrapper>
<Head>
	<title>React Chart Doc | OORSIG</title>
</Head>
## react-chartjs-2 library

## Introduction

`react-chartjs-2` is a React library that serves as a wrapper for the Chart.js library. It allows you to easily create and display various types of interactive charts in your React applications.

### Installation

To use the `react-chartjs-2`, you need to install the `react-chartjs-2` library:

```
npm install react-chartjs-2
```

## Example of react-chartjs-2

Here's an example of how to use the `LineChart`:

```tsx
import {
  Chart,
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend,
} from 'chart.js';
import { Line } from 'react-chartjs-2';

Chart.register(
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend
);

interface ChartDataset {
  label: string;
  data: number[];
  borderColor?: string;
  backgroundColor?: string;
  pointRadius?: number;
}

interface ChartData {
  labels: string[];
  datasets: ChartDataset[];
}

interface Axes {
  grid?: {
    display?: boolean,
    color?: string,
  };
  border?: {
    display: boolean,
    color?: string,
  };
  ticks?: {
    color?: string,
    fontSize?: string | number,
    fontFamily?: string,
  };
}

interface ChartOptions {
  responsive?: boolean;
  plugins?: {
    legend?: {
      display?: boolean,
      position?: 'top' | 'bottom' | 'left' | 'right',
    },
    title?: {
      display?: boolean,
      text?: string,
    },
  };
  scales?: {
    x?: Axes,
    y?: Axes,
  };
}


const options?: ChartOptions  = {
  responsive: true,
  plugins: {
    legend: {
      display: false,
      position: 'top',
    },
    title: {
      display: false,
      text: '',
    },
  },
  scales: {
    x: {
      grid: {
        display: false,
      },
      border: {
        display: false,
      },
    },
    y: {
      grid: {
        display: true,
        color: 'gray',
      },
      border: {
        display: false,
      },
    },
  },
};

const labels = ['January', 'February', 'March', 'April', 'May', 'June', 'July'];

const data:ChartData  = {
  labels,
  datasets: [
    {
      label: 'Open PRs',
      data: [100, 200, 300, 333, 89, 600, 700],
      borderColor: 'green',
      backgroundColor: 'green',
      pointRadius: 0,
    },
    {
      label: 'Closed PRs',
      data: [700, 600, 500, 400, 300, 200, 100],
      borderColor: 'red',
      backgroundColor: 'red',
      pointRadius: 0,
    },
    {
      label: 'Merged PRs',
      data: [400, 500, 500, 200, 100, 10, 50],
      borderColor: 'blue',
      backgroundColor: 'blue',
      pointRadius: 0,
    },
  ],
};

<Line options={options} data={data} />;
```

 <Line
        options={{
          responsive: true,
          plugins: {
            legend: {
              display: false,
              position: 'top',
            },
            title: {
              display: false,
              text: '',
            },
          },
          scales: {
      x: {
        grid: {
          display: false,
          color: '',
        },
        border: {
          display: false,
        },
         ticks: {
        color: 'gray',
        fontFamily: 'Inter',
      }
      },
      y: {
        grid: {
          display: true,
          color: 'gray',
        },
        border: {
          display: false,
        },
         ticks: {
        color: 'gray',
        fontFamily: 'Inter',
      }
      },
    },
        }}
        data={{
          labels: [
            'January',
            'February',
            'March',
            'April',
            'May',
            'June',
            'July',
          ],
          datasets: [
            {
              label: 'Open PRs',
              data: [100, 200, 300, 333, 89, 600, 700],
              borderColor: 'green',
              backgroundColor: 'green',
              pointRadius: 0,
            },
            {
              label: 'Closed PRs',
              data: [700, 600, 500, 400, 300, 200, 100],
              borderColor: 'red',
              backgroundColor: 'red',
              pointRadius: 0,
            },
            {
              label: 'Merged PRs',
              data: [400, 500, 500, 200, 100, 10, 50],
              borderColor: 'blue',
              backgroundColor: 'blue',
              pointRadius: 0,
            },
          ],
        }}
      />
</MDXWrapper>
